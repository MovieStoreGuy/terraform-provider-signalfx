package tftest

import "testing"

func TestNewHTTPMockStat(t *testing.T) {
	t.Parallel()

	for _, tc := range []struct {
		name string
	}{} {
		tc := tc
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()

		})
	}
}
